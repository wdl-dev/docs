{
  "name": "docs",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:content": "node scripts/build-content.mjs",
    "test": "node --test test/*.test.js",
    "deploy": "wdl deploy . --ns site",
    "prune": "node scripts/prune-versions.mjs",
    "dry-run": "wrangler deploy --dry-run --outdir=.deploy-dist"
  },
  "devDependencies": {
    "@wdl-dev/cli": "^1.5.0",
    "markdown-it": "^14.3.0",
    "markdown-it-cjk-friendly": "^2.0.2",
    "wrangler": "^4.111.0"
  }
}
